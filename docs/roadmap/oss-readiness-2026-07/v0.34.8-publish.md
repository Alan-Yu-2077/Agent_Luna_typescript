# v0.34.8 — Publish to `Luna-ts` (fresh clean history, owner-gated) 🔒

> **Status: PLANNED — 🔒 OWNER-GATED (going public).** Initiative: OSS Readiness (Order 9, version 9/9).
> Risk: **Medium** — first public exposure; the clean-clone dry-run must pass first. **Not irreversible**
> (Luna-ts can be deleted + re-published). Depends: v0.34.7 (working tree fully OSS-clean + dry-run green).
> Flag: **requires explicit owner go-ahead to go public.**

## Goal
Publish the scrubbed working tree to the **new public repo `github.com/Alan-Yu-2077/Luna-ts`** (fresh,
empty) as a **clean initial history**. Because Luna-ts starts empty, there is **no PII history to purge**
— the old irreversible `filter-repo` force-push against the private repo is not needed. The private
`Agent_Luna_typescript` (all 200+ commits, yumi, `neoru`, private soul) **stays private, untouched**.

## What ships (a publish, not a code change)
- **Squash the OSS-clean `oss-prep` tree to a single fresh initial commit** (`Initial public release —
  Luna vX.Y.Z`) on a new `main`, authored by a neutral `noreply` identity. (A curated multi-commit
  history is optional; the point is that **none of the private 200-commit history — with its Gmail /
  `MacBook.local` / owner-name / yunwu / quoted private chats — is inherited**.)
- **Push to `oss` (`Luna-ts`) `main`** — the clone already has the remote wired (`git remote add oss …`).
- **Repo settings** (owner, in the GitHub UI): description, topics, default branch `main`, LICENSE shown,
  Issues/Discussions as desired.
- Tag the release (e.g. `v0.34.8` / `v1.0.0-oss`).

## Verification (the go/no-go)
- **The v0.34.7 clean-clone dry-run passes** — a stranger cloning `Luna-ts` with no private sibling can,
  via `SETUP.md`, reach a running Luna (model + voice). This is the release gate.
- `git clone https://github.com/Alan-Yu-2077/Luna-ts` then across its (short, fresh) history:
  `git log --all -p | grep -iE 'alan|yualan|macbook|yunwu|/Users/alanyu2077|neoru'` → **0**;
  `git log --all --stat | grep -iE 'models/yumi|gpt-sovits-service'` → **0**.
- Full-history secrets scan → still **0**.
- `bun install && bun test && bun run build` green on the fresh public clone.

## What this version explicitly does NOT include
- Any rewrite of the private `Agent_Luna_typescript` repo (it is never touched). No behavior change.

## Risk
- First public exposure — gate strictly on the clean-clone dry-run + the grep checks above. Reversible:
  if something leaks, delete/re-init `Luna-ts` and re-publish (nothing in the private repo changed).
- Decide **squash vs curated history** — squash is simplest and guarantees no inherited PII; a curated
  history means hand-authoring commits from the clean tree (more work, no functional benefit for OSS).

## Acceptance criteria
- [ ] Owner has authorized going public.
- [ ] `Luna-ts` `main` = the OSS-clean tree; its full history greps clean (PII/yumi/`neoru`/yunwu → 0);
      secrets 0; a fresh public clone builds + tests green.
- [ ] The clean-clone dry-run (v0.34.7) passed on the published repo.

## Notes
- Future OSS development happens in `~/Desktop/Agent_Luna_oss` → `Luna-ts`; the private repo can keep
  its own (yumi/`neoru`/private-soul) development independently. Keeping the two in sync is a workflow
  decision for later (cherry-pick scrubbed changes forward, or develop OSS-first + overlay private
  assets locally).
