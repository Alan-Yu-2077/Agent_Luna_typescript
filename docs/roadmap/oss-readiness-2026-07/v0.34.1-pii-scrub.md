# v0.34.1 — PII scrub (owner name, handle, appId, UA)

> **Status: PLANNED.** Initiative: OSS Readiness (Order 2, version 2/9). Risk: **Medium** (fixtures
> assert the strings they contain; appId change moves the packaged bundle id). Depends: v0.34.0. Flag: none.

## Goal
Remove the owner's real identity (name, GitHub handle, real hobbies) from everything that ships in the
**working tree** — runtime prompts, test/script fixtures, source comments, the desktop appId, and the
outbound User-Agent. History is untouched (v0.34.8).

## What ships
- **Runtime prompt de-name** — `packages/server/src/dream/prompts.ts` (`personaUpdatePrompt` /
  `refineSemanticPrompt` few-shots, ~42/99/104/106/116): replace the owner's real name + real facts
  (Guilty Gear Shion, poker, NFA/DFA) with **invented, equally-concrete fictional** examples. Keep
  `memoryAuditPrompt:56` as the safe template. (Runtime code already injects the resolved owner name;
  no signature change.)
- **Comments → "the owner"** — the audit's `main.ts:142`, `smoke.ts:8`, `proactive/ladder.ts:5`
  **plus the workflow-found misses**: `proactive/cadence.ts:35`, `turn/weatherContext.ts:47`,
  `web/src/live2d/pixiLive2DSink.ts:186/194`, `web/src/ui/speechStackView.ts:24`.
- **Desktop appId** — `packages/desktop/package.json:20` (`dev.alan.luna` → neutral reverse-DNS, e.g.
  `app.luna.desktop`). ⚠️ changes the macOS `app.getPath('userData')` path — note in release notes
  (existing local settings/DB orphan under the old id).
- **User-Agent** — `packages/server/src/tools/web/safeFetch.ts:26` broadcasts the owner's GitHub handle
  on every `web_fetch`; make it neutral + env-overridable (uses the v0.34.0 repo URL).
- **Fixtures/scripts** — rename owner name/facts in `l3.test.ts`, `soulSeed.test.ts`,
  `provider/anthropic.test.ts`, `recall/*.test.ts`, `tools/builtin/{recall,remember}.test.ts`,
  `turn/runTurnResilience.test.ts`, `scripts/proactive-soak.ts`, `scripts/integrity-sweep.ts` —
  changing arrange **and** assert together per test.

## Tests
- Acceptance gate is a **grep sweep**, not the enumerated list: `grep -rn -e Alan -e Shion -e "住上海" packages/ scripts/` → 0.
- `bun test packages/server` green (fixtures renamed input+expectation together — a one-sided rename fails).
- `tsc` clean.

## What this version explicitly does NOT include
- yunwu (v0.34.2), abs-paths (v0.34.3), locale strings (v0.34.4), docs (v0.34.5 removes them so no
  doc scrub here), history (v0.34.8).

## Risk
- Fixtures use PII as both input and expected output — rename atomically per test. Grep-gate to catch
  the audit's undercount.

## Acceptance criteria
- [ ] Owner name/handle/real-hobby strings gone from `packages/` + `scripts/` (grep = 0).
- [ ] `bun test` green; `tsc` clean; appId neutral; UA neutral+overridable.

## Notes for next
- Release notes must mention the userData-path move from the appId change.
