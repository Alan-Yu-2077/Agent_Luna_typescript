# v0.34.5 — Docs restructure (remove internal diaries + .claude, extract clean docs)

> **Status: PLANNED.** Initiative: OSS Readiness (Order 6, version 6/9). Risk: **Low** (docs/tooling
> only — but the README + docs index link INTO removed files, so both indexes must be rewritten in the
> same version or they 404). Depends: v0.34.1, v0.34.3. Flag: none.

## Goal
Remove the internal dev diaries + private author tooling (dense with the owner's name, machine paths,
yunwu, cost anecdotes, and quoted private chats — 36 docs match) from the public tree, and leave a
clean, PII-free architecture + roadmap in their place. A whole-file removal is why a line-by-line docs
scrub is unnecessary.

## What ships
- **Remove from the public tree:** `docs/history/DEVELOPMENT.md`, `docs/REWRITE_CONTEXT.md`, the entire
  `docs/roadmap/**` (~90 files / 24 dirs — **including this initiative folder**), and `.claude/`
  (author agent tooling: abs paths, yunwu, homebrew bun, skill docs).
- **Extract `ARCHITECTURE.md`** (PII-free, structure only): the monorepo layout (protocol/server/web/
  desktop), the 3-layer SQLite memory (L1 window / L2 turns / L3 facts) + dream consolidation + the soul
  file, the tool registry + concurrency model, the proactive rails (silence ladder + continuation), and
  the perception subsystems (time / weather).
- **Extract `ROADMAP.md`** (high-level, public themes only — no per-version private history).
- **README structural pass** — fix `README.md:34` "Layout (planned)/to be created in v0.1" → the real
  four-package tree; add a LICENSE section; scrub the handle (line 3) + abs path + Python-head line
  (58-61); repoint "further reading" at `ARCHITECTURE.md`/`ROADMAP.md`. **Rewrite `docs/README.md`**
  index (it links into the removed files). **Do NOT** add a `docs/SETUP.md` link — that file is authored
  in v0.34.7 (a link now would 404).

## Tests
- Link check: no README / `docs/README.md` link points into a removed path (else 404). Grep for
  `DEVELOPMENT.md`, `REWRITE_CONTEXT.md`, `roadmap/` references in surviving markdown.
- `bun test` / `tsc` unaffected (no code).

## What this version explicitly does NOT include
- The `docs/SETUP.md` new-user guide (v0.34.7). The git-history copies of these diaries (v0.34.8 — they
  live on in history even after removal, which is the strongest argument for the rewrite).

## Risk
- **README/index 404 trap** — removing files without rewriting both indexes in the same version yields
  dead links. Land the removal + both index rewrites atomically.

## Acceptance criteria
- [ ] `docs/history`, `docs/REWRITE_CONTEXT.md`, `docs/roadmap/`, `.claude/` gone from the working tree.
- [ ] `ARCHITECTURE.md` + `ROADMAP.md` present, PII-free (grep = 0).
- [ ] No dangling markdown links.

## Notes for next
- This removes the diaries from the working tree; because v0.34.8 publishes the clean tree to a **fresh**
  `Luna-ts` (not a rewrite of this repo), those diaries never enter the public history at all — the
  private repo keeps them.
