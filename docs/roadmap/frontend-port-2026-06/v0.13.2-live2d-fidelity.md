# v0.13.2 — Live2D high-fidelity FaceVM

> **Status: ✅ SHIPPED 2026-06-14.** Initiative: Frontend Port — the Body (Order 6, version 4/6).
> Risk: **Medium**. Depends: v0.13.1 (foundation). Flag: none.
>
> **Shipped note:** the 14 emotions (entry→sustained poses + intro/perform/outro timelines),
> soft-blend/hard-replace + channel ownership, overlay special-params (blush/dark-face/teary), and the
> 9 referenced actions landed behind the v0.13.1 `Live2DSink`; the affect→emotion map lives in the
> frontend. **Deferred:** per-emotion sine micro-motion, the 6 procedural idle profiles, the 36
> unreferenced actions, rich speaking/thinking procedural motion. Truth source:
> [DEVELOPMENT.md](../../history/DEVELOPMENT.md) → v0.13.2.

## Goal

Replace v0.13.1's first-cut FaceVM internals with a faithful port of Python's layered engine, so
yumi's expressions/states have the original richness — behind the **same `Live2DSink`** (no
controller or `app.ts` changes). This is the "尽量高保真" half of the Live2D work, split out from
v0.13.1 because the libraries are ~3200 lines.

## What ships
- Port `js/runtime/face-vm.js` layer model: idle profile → state → emotion → action → speech →
  smoothing → flush (the ordered stack), per-state smoothing curves, mouth-owner locking.
- Port `js/runtime/layers/emotion-library.js` (intro/perform/outro timelines, owned channels,
  `actionRefs`/`overlayRefs`) + `action-library.js` (keyframed micro-actions).
- Rich **affect → emotion** mapping: the 15 `ExpressionKey`s → emotion-library entries (+ special
  params `Paramheart`/`Paramxingxing`/etc.), replacing `expressionMap.ts`'s first-cut poses.
- Idle profiles (defaultIdle / cuteSway / …) optionally superseding the model's built-in idle.

## Tests
- Emotion timeline scheduler (intro→perform→outro) advances correctly over an injected clock.
- Action keyframe interpolation hits expected param values at sampled times.
- Affect→emotion resolution covers all 15 keys; specials fire.

## Not included
Audio/lip-sync → v0.13.3. Dream overlay / UX polish → v0.13.4.

## Acceptance
- [ ] Expressions visibly match the Python reference's character; states layer + smooth cleanly.
- [ ] All 15 affects resolve to a distinct emotion; special effects (hearts/stars) appear.
- [ ] `tsc` + tests green; browser smoke against the reference look.

## Notes for v0.13.3
Keep `faceVm.setMouthOpen` as the single lip-sync entry point; the emotion layer must not fight the
speech mouth-owner (port the Python `mouthOwner` lock).
