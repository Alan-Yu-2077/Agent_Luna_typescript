# v0.23.1 — OpenAIProvider: translation core + complete + correctness-first chat

> **Status: PLANNED.** Initiative: OpenAI-protocol adapter (Order 16, version 2/4). Risk: **Medium**.
> Depends: v0.23.0. Flag: `LUNA_PROVIDER=openai` (default `anthropic`).

## Goal

Land the **Anthropic⇄OpenAI translation core** (pure, exhaustively unit-tested) and a working
`OpenAIProvider` with `complete()` + a **correctness-first `chatStream()`** (non-streaming under
the hood — emits one `message_stop`). End of this version: a full reactive turn runs end-to-end on
an OpenAI-protocol model. Streaming/latency is deliberately v0.23.2 — correctness before speed.

## What ships

- **`provider/openai/translate.ts` (NEW, pure functions — the riskiest surface, no I/O):**
  - `toOpenAIRequest(req: ProviderRequest)` → `{ model, messages, tools?, max_tokens|max_completion_tokens }`:
    - `system` (string or `Anthropic.TextBlockParam[]`, **stripping `cache_control`**) → a leading
      `{role:'system', content}` message (or `developer` per capability, v0.23.2).
    - `Anthropic.MessageParam[]` → OpenAI messages: `user` text blocks → `{role:'user', content}`;
      assistant `text` blocks → `{role:'assistant', content}`; assistant `tool_use` blocks →
      `assistant.tool_calls[]` (`{id, type:'function', function:{name, arguments: JSON.stringify(input)}}`);
      `tool_result` blocks → `{role:'tool', tool_call_id, content}`.
    - `Anthropic.Tool[]` (flat-root `input_schema`) → `tools[]` (`{type:'function', function:{name,
      description, parameters}}`) — inherits the flat-root constraint (REWRITE_CONTEXT v0.5.2).
  - `fromOpenAIMessage(choice)` → a **synthesized `Anthropic.ContentBlock[]`** (`assistantContent`):
    assistant `content` → a `text` block; `tool_calls[]` → `tool_use` blocks (`{type:'tool_use',
    id, name, input: JSON.parse(arguments)}`, with the same `_noargs`/bad-JSON tolerance as
    `unwrapGatewayInput` at [`anthropic.ts:18-35`](../../../packages/server/src/provider/anthropic.ts)) — so `runTurn.ts:364`/`498-514` replay is unchanged.
  - `toProviderToolUses(choice)` → `ProviderToolUse[]` for `message_stop.toolUses`.
- **`provider/openai/openaiProvider.ts` (NEW): `OpenAIProvider implements Provider`** —
  constructor `{apiKey?}` honoring per-instance keys (the dream cascade), `baseURL` from
  `LUNA_OPENAI_BASE_URL` (fallback `ANTHROPIC_BASE_URL` for the shared gateway), model from
  `LUNA_MODEL`. `capabilities = { thinking:false (v0.23.2 flips for reasoning models), promptCache:false,
  interleavedToolStreaming:false (v0.23.2→true), toolUse:true, systemRole:true, maxOutputTokens }`.
  - `complete(req)` → non-streaming chat-completions call → `{text, usage}` (map `usage.prompt_tokens`/
    `completion_tokens` → `input_tokens`/`output_tokens`).
  - `chatStream(req)` → a single non-streaming call, then yield (optionally) one `text_delta` with
    the full text + a `message_stop` with the synthesized `assistantContent` + `toolUses` + `usage`.
    Honors `req.signal`.
- **HTTP client:** a minimal `fetch`-based client (mirrors the embeddings standalone-fetch pattern,
  LD #13) — no new SDK dependency. A `setOpenAIFetcher()` test seam (like `setWeatherFetcher`).
- **`providerFor()`:** flip the `openai` branch from throw → `new OpenAIProvider(opts)`.

## Tests

- `provider/openai/translate.test.ts` (the core): a multi-turn fixture with user text, an assistant
  `tool_use`, a `tool_result`, and a final assistant text **round-trips** Anthropic→OpenAI→
  synthesized-Anthropic with no loss; `cache_control` is stripped from system; bad-JSON tool
  arguments tolerate (no throw); a tool with a flat-root schema maps to a `function.parameters`.
- `provider/openai/openaiProvider.test.ts`: with an injected fetcher, `complete()` returns mapped
  text+usage; `chatStream()` yields a `message_stop` whose `assistantContent` replays into a valid
  `session.history` entry; a tool-call response surfaces in `toolUses`.
- `runTurn.test.ts` (extend, provider-parameterized): a `runTurn` driven by a mocked `OpenAIProvider`
  completes a turn (text + one tool call + `message`) and persists correct history.

## What this version explicitly does NOT include

- **Real token streaming / interleaved tool-use** (chatStream is non-streaming here) → v0.23.2.
- Reasoning-model `thinking` + `developer`-role / token-param quirks → v0.23.2.
- Model registry / multi-model selection → v0.23.3.

## Risk

- **Medium** — translation correctness (esp. the tool_use/tool_result round-trip + history replay)
  is the crux. Mitigated by making translation **pure + fixture-tested before** any network code,
  and by the injected fetcher (no live dependency in CI).
- A turn with no token streaming will feel laggy on long replies — acceptable for one version
  (default path is still Anthropic); v0.23.2 restores streaming.

## Acceptance criteria

- [ ] `translate.ts` round-trips a tool-using multi-turn fixture losslessly (unit-tested).
- [ ] `OpenAIProvider.complete()` + `chatStream()` work against an injected fetcher; `apiKey` is
      per-instance (summarizer-key isolation preserved).
- [ ] `LUNA_PROVIDER=openai` drives a full `runTurn` (mocked) — text + tool + `message` — with valid
      persisted history.
- [ ] `LUNA_PROVIDER=anthropic` default unchanged; `bun test` green, `tsc` ×3 clean.

## Notes for v0.23.2 (don't foreclose)

- Keep `chatStream` structured so the non-streaming body is swapped for an SSE reader without
  changing the emitted `ProviderEvent` sequence shape.
- The synthesized-`assistantContent` builder is reused by the streaming path (accumulate deltas →
  the same blocks) — keep it a standalone function.
