{
  "name": "@luna/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./index.html --production --outdir=dist && bun run build:assets",
    "build:assets": "cp public/live2dcubismcore.min.js dist/ && cp -R public/models dist/models"
  },
  "dependencies": {
    "@luna/protocol": "workspace:*",
    "pixi-live2d-display": "0.5.0-beta",
    "pixi.js": "7.4.2",
    "zod": "^3.25.0"
  }
}
